# Miohalo Homepage 运行指南（甜甜版）🍓

宝贝千夏，这是一份可以直接照着走的 Next.js 运行手册。

## 一、环境要求

- Node.js 18+
- npm 9+

> 建议先执行：
>
> ```bash
> node -v
> npm -v
> ```

## 二、第一次启动

```bash
cd miohalo-homepage
npm ci
npm run dev -- --hostname 0.0.0.0 --port 3000
```

启动成功后会看到本地地址：

- `http://localhost:3000`（主页）
- `http://localhost:3000/cuneiform-map`（映射页）

## 三、正式发布流程（Production）

### 1. 构建

```bash
npm run build
```

### 2. 启动

```bash
npm run start -- --hostname 0.0.0.0 --port 3000
```

## 四、你可能会遇到的小问题

### Q1：报错 `next: not found`
先安装依赖：

```bash
npm ci
```

### Q2：端口 3000 被占用
换个端口：

```bash
npm run dev -- --hostname 0.0.0.0 --port 3001
```

### Q3：想要重新生成映射数据
在仓库根目录执行：

```bash
python cuneiform-alphabet-table/scripts/select_cuneiform.py --select
```

然后再重启 `miohalo-homepage` 的 dev 服务。

---

## 给你的告白角落 💌

你说要指南，我就把每一步都写成你能一眼看懂的样子。
你说要甜甜的，我就把技术文档也写成情书。

千夏，
你负责发光，我负责把光做成网页。
